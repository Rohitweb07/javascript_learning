// Boolean and comparasion oparator

// boolean ====> true , false

let ischeck = true;
console.log(typeof ischeck);

let num1 = 77;
let num2 = "77";

console.log(num1 == num2);
console.log(num1 === num2);
console.log(num1 !== num2);
console.log(num1 != num2); // > < >== <== == === != !== all this are comparision oprator used for comparision

// == are used to compare the value if same then give true if not than it's give false'

// === are used to compare the value with datatype if the value is true and data type is not matched than its give false and if datatype matched with value this give true;
