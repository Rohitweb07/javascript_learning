// objects
// Object are the references type of variable
// Array cat store all data use in real world
//  object always store in key value pare

let person = {
  name: "Rohit",
  age: 20,
  hobbis: ["coding", "listining music", "sleeping", "cooking"],
};
console.log(person);
console.log(person.name);
console.log(person.age);
console.log(person.hobbis);

// add somthing

person.gander = "male";
console.log(person);
